%h1 Editing signup

%div.row
  = render 'form'

%div.row
  = link_to 'Destroy', [@event,@signup], :confirm => 'This will delete the signup and all associated events. Are you sure?', :method => :delete
  \|
  = link_to 'Show', @event
